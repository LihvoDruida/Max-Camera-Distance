<Ui xmlns="http://www.blizzard.com/wow/ui"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.blizzard.com/wow/ui ..\FrameXML\UI.xsd">

    <!--
        No-lib/source-safe manifest.

        This archive only bundles LibCamera. Ace3, LibMountInfo, LibDataBroker,
        and LibDBIcon are optional runtime integrations loaded through LibStub
        when they are provided by a proper release packager or standalone addons.

        Do not add XML includes here unless the referenced files physically exist
        in the zip. scripts/verify-release-zip.py will fail the artifact if this
        manifest points to missing files.
    -->
    <Script file="LibCamera\LibCamera.lua"/>

</Ui>
